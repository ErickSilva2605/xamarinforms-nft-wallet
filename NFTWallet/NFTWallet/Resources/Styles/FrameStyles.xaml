<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary
    x:Class="NFTWallet.Resources.Styles.FrameStyles"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pancake="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
    >

    <x:Int32 x:Key="FrameCornerRadiusSize">8</x:Int32>

    <!--  Card Style  -->
    <Style x:Key="CardButton" TargetType="{x:Type pancake:PancakeView}">
        <Setter Property="HeightRequest" Value="50" />
        <Setter Property="CornerRadius" Value="{StaticResource FrameCornerRadiusSize}" />
        <Setter Property="HorizontalOptions" Value="Fill" />
    </Style>

    <Style x:Key="SettingsCard" TargetType="{x:Type pancake:PancakeView}">
        <Setter Property="Padding" Value="16,0" />
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Dark={StaticResource ColorPrimaryDark800}, Light={StaticResource ColorPrimaryLight50}}" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="HeightRequest" Value="56" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>
</ResourceDictionary>